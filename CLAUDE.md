# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository. Don't ever change this file; rather read / change note files following the instructions below:

- For general guidance on the codebase, see `REVIEW.md`.

- For newly added functionalities, see `CHANGELOG.md`.

- For data preprocessing without Pangolin dependency, see `PYTHON_PREPROCESSING.md`.

- For training data preparationa nd generation, see `TRAINING_DATA_GENERATION.md`. 

To run the python code, please first activate the conda environment `ml_env` via `conda activate ml_env`. Do not run python code in conda base or a bare python environment!

